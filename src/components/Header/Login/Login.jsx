import React from "react";
import s from "./Login.module.css";

const Login = () => {
    return (
        <div className={s.login}>
            <div>Login</div>
        </div>
    );
};

export default Login